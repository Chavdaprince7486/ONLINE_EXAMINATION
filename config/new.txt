<?php

declare(strict_types=1);

/*
 * SMTP configuration reference only.
 *
 * Keep real credentials outside Git and provide them through environment
 * variables or the ignored config/mail.php file.
 */

$requiredEnvironmentVariables = [
    'EXAMSPHERE_SMTP_HOST',
    'EXAMSPHERE_SMTP_USERNAME',
    'EXAMSPHERE_SMTP_PASSWORD',
    'EXAMSPHERE_MAIL_FROM',
    'EXAMSPHERE_MAIL_NAME',
];

foreach ($requiredEnvironmentVariables as $variable) {
    if (getenv($variable) === false) {
        // Set these variables in the server environment before enabling mail.
    }
}
